* write external tests

* refactor parser
    * remove validate input
    * put for loop in reverse order to avoid break
    * if inside for should be its own function
    * pushing inside listOfOptions should be its own function


