* write external tests

*** alternate parser
    *** could use option = args.filter(startsWith-)
    *** filenames = args.filter(doesnot startsWith - )
    *** but wont pass n wc.js -l file1 -a file2

* refactor parser
    * a function should take listOfOptions/parsedOptions
    * and return options - lineCount, wordCount, charCount

